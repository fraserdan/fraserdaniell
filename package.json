{
  "name": "fraser-daniell",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:cms": "tinacms dev -c \"astro dev\"",
    "build": "astro build",
    "build:prod": "tinacms build && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "tinacms": "^2.0.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
